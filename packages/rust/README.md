# quanttide-work Rust 库

知识工作领域 Rust 语言工具包。

## 安装

在 `Cargo.toml` 中添加：

```toml
[dependencies]
quanttide-work = "0.1.0"
```

## 使用

```rust
use quanttide_work::{DOMAIN, VERSION};

println!("{DOMAIN} v{VERSION}");
```

## 测试

```bash
cargo test
```

## 许可

CC-BY-4.0
