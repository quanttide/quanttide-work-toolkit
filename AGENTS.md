# AGENTS.md - quanttide-work-toolkit

在本仓干活的约定。先是一条分工，然后两条判断线：文档怎么写、接口怎么设计。反面例子都取自本仓真实评审，明细见 issue「工具箱接口设计问题」。

## 分工

**平台管位置，工具库管内容。**

- 位置是平台的事：资源从哪读、写到哪、分散在几处，由端侧装载时决定——工作区不是一个目录，怎么组合物理资源是平台的事。
- 内容归工具库：模型、语法、不变量、取值。工具库不建模位置，也不存位置。
- 同一份模型可以来自任何目录、任意组合；位置怎么变，工具库不动。

这条是接口的底色：凡是「东西在哪」的问题，都不进工具库。

## 文档写作

写给平台开发者看的，用技术写作的语气，不用口语。

好的样子：

- 陈述句、第三人称：写「定义不合法时返回 Err」，不写「不合法当场抛」。
- 一段一件事；信息多就拆段，或加 `###` 小标题分段。
- 每段最多一处加粗，列表项里不加粗。
- 出现 `payload` / `data` 这类参数，当场讲清它是什么类型、代表什么。
- 代码块前后有说明或小标题，不背靠背堆两组代码。
- 中文引号用「」，术语全文一致。

不好的样子：

- 口语词：「话头」「跑腿」「那份就是这么做的」「当场」。
- 一段塞五件事，靠连续加粗撑住主线。
- 参数不解释，读者不知道传什么。
- 代码块与说明脱节，读者回头找归属。
- 用英文直引号，或拿引号包整句。

## 接口设计

工具箱只装不因平台而变的核心逻辑，不含 IO。接口要让人一眼看懂「传值还是传文件」「哪一份是正本」。

好的样子：

- 值 / 出处分离：模型收解析后的值，不收文件；文件名只作报错标签。
- 不碰文件系统：需要文件事实时用回调注入（如 `check(..., exists)`）。
- 单一事实源：版本由清单注入（Rust 用 `env!("CARGO_PKG_VERSION")`），不手写。
- 两端同名同义，用契约向量把结论钉住。
- 每个模型都有校验入口，或明确「不校验」并写进规范。

不好的样子：

- 命名撒谎：`from_yaml` 收的是值不是文本；同一语义两端名字不同。
- 一个构造器兼三职：定身份、做校验、建模型。
- 双事实源：`name` 既取自字段又取自文件名，且从不校验二者一致。
- 裸字符串当出处：`file` / `place` 手拼，没有类型。
- 类型在边界丢掉：把密封类型压成 `kind` + 位置 `args`。
- 错误类型太薄：只有一个字符串，调用方无法定位。
- 校验覆盖不均：五个模型只一个设门。
- 模块依赖成环，内部助手被 `pub` 成公共出口。
